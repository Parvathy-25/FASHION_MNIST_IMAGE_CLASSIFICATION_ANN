# FASHION_MNIST_IMAGE_CLASSIFICATION_ANN
This project implements an Artificial Neural Network (ANN) to classify Fashion-MNIST images into 10 clothing categories. The images are preprocessed, flattened, and trained using dense layers with softmax output to perform multiclass image classification.
